---
import Container from "@/components/ui/Container.astro";
import Section from "@/components/ui/Section.astro";
import type { TrackRecordContent } from "@/content/pages/track-record/types";

interface Props { content: TrackRecordContent["faq"] }
const { content } = Astro.props;
---

<Section surface="dark" spacing="lg" ariaLabelledby="tr-faq-h2">
  <Container size="md">
    <header class="text-center">
      <h2 id="tr-faq-h2" class="text-3xl sm:text-4xl font-bold leading-tight tracking-tight text-white-pure">{content.h2}</h2>
    </header>

    <div class="mt-10 divide-y divide-deep-navy-600 rounded-2xl border border-deep-navy-600 bg-charcoal-700">
      {content.questions.map((item) => (
        <details class="group" name="track-record-faq">
          <summary class="flex items-center justify-between gap-4 cursor-pointer px-6 py-5 list-none text-left focus-visible:outline-none focus-visible:bg-deep-navy-800 rounded-2xl">
            <span class="text-base lg:text-lg font-semibold text-white-pure">{item.q}</span>
            <svg class="h-5 w-5 shrink-0 text-white-muted transition-transform duration-200 group-open:rotate-180" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
              <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd" />
            </svg>
          </summary>
          <div class="px-6 pb-5 text-base leading-relaxed text-white-soft">{item.a}</div>
        </details>
      ))}
    </div>
  </Container>
</Section>

<style>
  details > summary::-webkit-details-marker { display: none; }
  details > summary::marker { content: ""; }
</style>
